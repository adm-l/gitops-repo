apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-api-dev
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/adm-l/develop.git
    targetRevision: dev
    path: hello-api-chart

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
